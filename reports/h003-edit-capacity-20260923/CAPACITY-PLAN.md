# H003 guarded edit capacity — source checkpoint

User guarded-editing acceptance: **APPROVED**. The original same-seed42 teapot
regression remains **FAIL**; pass43 is a retained counterpart, not an intrinsic
model repair. This new task has **0 accepted capacity calls / maximum4**,
0 dispatches and0 staging attempts. The closed diagnostic task remains6 attempts /
5 actual GPU requests. Its remaining slot is not available to this task.

WINDOW-RELEASE.json releases Worker2 ownership but grants no inference in this
turn. COORDINATOR-NOTE.md requires a source/preflight checkpoint, exact root review
before requests/activation, and visual review before any edit profile is enabled.
The private runner is source-only and has not been invoked. Resume the same native session.

## Fixed planned cases

REQUEST-LEDGER.json contains exact prompts, seeds, input/working hashes, geometry,
recipe and success criteria. It is a predeclaration, not execution evidence.

| Case | Separate profile | Seed | Input and purpose |
|---|---|---:|---|
| C01 | edit /1ref /1024x1024 |46| Original teapot; exact red-to-blue instruction |
| C02 | edit /1ref /1536x864 |47| Diagnostic0.8 LakeBled working copy; blue church nave roof |
| C03 | edit /1ref /1920x1080 |48| Original LakeBled; same local roof edit; internal bottom8pad/crop |
| C04 | edit /2refs /1024x1024 |49| Independent teapot and retained NASA astronaut; portrait contribution on teapot |

C02 is explicitly documented diagnostic-only uniform LANCZOS preparation, allowed
by the task instruction. LakeBled is a retained model-generated image at native
public1920x1080, not a natural photograph. No product resize approval is inferred.
All originals remain at their prior task paths. NASA attribution and prior fixture
preparation are preserved. Seeds46..49 do not collide with known ancestry42 or
23092301; unknown imported provenance remains a limitation.

Native recipe is unchanged: pinned Qwen-Image-2.1/SGLang runtime image,
torch_sdpa/eager/all-resident; steps40, guidance_scale1, true_cfg_scale1,
generator_device=cpu, n1, opaque PNG. No masks, transparency, settings sweeps,
retries, extra fixtures generated by GPU, or inference this turn.

## Geometry/source audit

Current installed source was read without model/framework imports. Native
QwenImage21EncodingStage resizes every reference to target area, rounded to32.
Sending raw1920x1080 with target1920x1088 stretches the reference. The candidate
validates the public input, applies native-equivalent EXIF orientation and RGBA
normalization (including tRNS), verifies canonical1920x1080, then creates a separate
PNG with exact canonical pixels plus8 copies of the last row. Orientation6/8 that
swaps dimensions is rejected. The existing top1080 output crop is preserved.
Only opaque one-reference FHD mapping is added; two-reference FHD stays refused.
Identity1024 and1536 inputs retain exact bytes/order. Capabilities disclose edit
input_padding; no manifest gains profiles merely from these source changes.

Checkpoint processor is Qwen3VLProcessor/Qwen2VLImageProcessorFast. The installed
AutoProcessor loader passes no resize overrides; AutoImageProcessor resolves the
legacy Fast suffix to the Torchvision implementation. Its checkpoint patch16 ×
merge2 and65536..16777216pixel bounds preserve all proposed working dimensions.
Torchvision returns the same tensor for equal dimensions before normalization.
Exact installed smart_resize was executed offline for all4 geometries; source
hashes and findings are in artifacts/source-audit.json. No numerical repair claim.
VAE reference/target token counts are4096/4096,5184/5184,8160/8160,8192/4096;
these counts are not memory qualification or a safe dispatch estimate.

## Next reviewed execution proposal

The final root-reviewed procedure proposal in open_capacity_window.py sends
normal SIGTERM only to the verified API MAIN process, after verifying Restart=no.
It never invokes systemctl stop, forced kill or restart. No lifecycle lease is
held while waiting at most30seconds for graceful exit, allowing a just-admitted
request's existing recovery to finish without lock contention. Require clean
exited/status0/success, MainPID0, old PID absent and loopback30006 listener gone.
Only then acquire the canonical lease, recheck guards and unchanged image plus
both text model/container/start/GPU identities, and durably write the window
receipt under that same lease. Timeout, nonclean exit or backend change yields
no successful receipt and no private request. This replaces the earlier stop-
under-lease proposal and its failed-recovery ambiguity. No shutdown was invoked.

The API remains deliberately stopped across the private cases. Every case binds
the exact protected H003-CAPACITY-WINDOW.json hash; the runner independently
checks the stopped wrapper and original model identities. The initial receipt
cannot authorize another case after an unreceipted or ambiguous prior dispatch.

Next GPU call is C01 ONLY. capacity_runner.py rechecks the stopped wrapper and
window-bound identities under the canonical /run/llmctl/lifecycle.lock and current
registered/root storage guards. candidate_codec.py executes reviewed protocol source
in the EXISTING /data/services/image-api/venv/bin/python for preparation/delivery;
no package/source installation or model import. Strict working hashes are checked
before dispatch. This avoids native Pillow11.3 versus adapter Pillow12.3 codec
ambiguity. A private case selector is explicitly UNQUALIFIED, never a public manifest.

Reuse native open_run_directory/O_NOFOLLOW/write_exclusive/fsync for ordered bounded
fixtures and prepared bytes under H003-CAPACITY-Cnn in the current guarded invocation.
The stdlib-only capacity_probe.py sends one fixed native request. TERM840s/KILL5s
runs INSIDE the container, outer855s; no retry/replay. Preserve bounded raw native
response and decoded bytes before adapter validation, then save delivered PNG.
Known HTTP errors retain status; ambiguous dispatch reserves its slot until reviewed.
Predispatch CPU/staging failures count separately, and the wrapper remains deliberately
stopped for all cases rather than reopening generation.

Continuous .25s-target sampling starts before dispatch through settle. Capture all
three device free/total and UUID/PIDs, host available>=15%, all three model cgroups'
memory.current/max, swap.current, memory.events/swap.events and fresh global swap
deltas. Existing q1 swap148MiB/near32GiB cap is baseline, not zero-swap evidence.
Any new reserve/event/runtime failure stops the ladder. Allow the sole in-flight
request its fixed bounded settlement; killing its HTTP client cannot cancel GPU
work. No backend restart or model/text limit tuning is permitted per case.

Successful private settlement requires exit0, complete native response plus adapter
decode/delivery, no owned probe process or extra GPU context, unchanged window-bound
model identities, API still stopped, fresh guards/reserve and completed/fsynced
telemetry. Finally retains a receipt even after observation/storage failure; no
runner/settlement/telemetry/evidence error may pass. Per-case status is deliberately
API_STOPPED_BACKEND_HEALTHY, not generationready. The separate final activation
proves public readiness. No automatic per-case lifecycle calls exist. Any prior unreceipted attempt, ambiguous
dispatch or failed runtime/memory settlement blocks every later case, including
C04, under this window; the initial stop receipt cannot authorize overlap.

Eleven offline runner/codec checks cover false admission from an active/deactivating
wrapper, timeout/kill/unknown exits, failed identity/guard/settlement, late errors,
finalized telemetry, fresh swap/reserve detection, UUID/PID comparison and actual
candidate codec prepare/deliver. In-container timeout execution and live Linux
SIGTERM drain remain unexecuted; fixtures do not establish live qualification.

C02 requires C01 fidelity+reserve pass; C03 requires C02 pass. Before either larger
case, and independently before C04, root reviews a conservative headroom forecast
from the preceding measured peaks. If forecast freeAda<5%, record NOT_TESTED /
predispatch refusal. C04 uses C01 bound independently; single-ref results never
qualify it. No downscale/settings change or extra benchmark to force a pass.
Every enabled profile requires its own measured reserve and root visual acceptance.

## Exact final activation/manifest path

Latest root note permits **one deliberate existing recovery/reload plus its fixed
warmup**, separately accounted from4 capacity calls; no warm-adoption mechanism.
Preferred order is one reviewed clean wrapper stop, private candidate qualification, root image/metrics
review, then one final deployment of source AND only passed profile records together.
No lifecycle action is authorized in the current turn.

Protected manifest is /etc/llm-server/image-api.json. serve.py reads it once in
installation()/qualification() before create_app(); the app retains that in-memory
config. There is no supported dynamic manifest refresh. Changing a manifest alone
does not update live capabilities. Therefore the final profile refresh requires
the existing stopped-wrapper install/start path (the one clean stop occurred before qualification). Lifespan reconcile
invokes /usr/local/libexec/llm-image-backend-recover, which uses the current canonical
lease/guards, reset_owned(), systemctl restart llm-image-backend.service and fixed
warmup, then resident verification. Record intentional old/new image container/PID;
preserve both text container/PID/start/context/settings exactly. Root reviews the
final measured manifest hashes and exact install transaction before this action.
No second reload is planned and no crash reconciliation is weakened. Existing FHD
install tooling has hardcoded old hashes/six-generation profiles and must be
adapted narrowly after measured results, never run unchanged.

Private native+adapter qualification is separate from published API end-to-end
acceptance. Worker2 subsequently owns published-path acceptance. No harness,
engine, nginx, runtime, weights, drivers or model settings are changed here.

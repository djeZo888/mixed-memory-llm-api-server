# Closed 480K CPU SOURCE PREP — 2026-09-20

Prepared in the isolated Worker1 copy from reviewed46e7f29c6edd98c156006736b999e6c858a0c9ab. No VM, Proxmox, Worker2, model, inference, profiling, deployment or publication action occurred; no PREP measurement clock exists. This is a new closed benchmark identity. The completed old campaign and deferred G480/Q700 source/evidence remain intact.

The implementation adds a narrow adapter over existing concurrent runner/host/ownership, counter, fixture, safety and restoration paths. Exact A G96/Q16 and B G88/Q8 subsets configure both pools480000. Five fixed measurements and four discarded32-output warmups are admitted; G480K occupied fitting is rejected. Common Q256K follows A near-Q immediately and records actual peer condition/overlap. Fixed schema/sample/fixture identities, native pool/input proof, caps/reserves, speed-not-stop and strict-versus-one-fence scoring remain explicit.

Latest durable root refinements are incorporated: factor1.15 ESTIMATE headroom via exact policy metadata,4500-second future RUN at root dispatch, common-Q sequencing, pre-long CPU evidence from existing warmups, cheap runtime thread count, exact loading-only GPU timeout allowance. The narrow future acceptance check preserves absent-policy legacy25% field semantics; no production declaration/API/profile or accepted receipt is changed/fabricated.

CPU evidence uses bounded process/cgroup/per-vCPU counter reads, PSI, quota/throttling and honest event-proxy phases; native timing is separate. NUMA pages are quiescent only. Missing CPU warmup evidence stops before long requests with an exact UNAVAILABLE receipt. Complete inference does not upgrade missing/sparse CPU evidence to a complete comparison. Q8 is an affinity budget, not eight runtime threads.

Verification:196 affected offline tests PASS (final consolidated invocation), including non-network constructor/checkpoint/prepare-job and missing package checks; native observations in tests are synthetic. Command is retained in task-local test-receipt.json and affected-tests.log. Inert CLI help and whitespace checks pass. Independent review corrected diagnostic failure bypass, result-pending peer labeling, exact-five dispatch guard, CPU gap matching and fixture-target closure before the final check.

One earlier exploratory regression invocation exposed an unchanged legacy candidate-validation test expectation: float available-memory input is rejected as unavailable before its expected insufficient-reserve code. The unchanged helper/test are outside this new fix; this is retained in task-local integration-initial-failures.log and explicitly excluded from the final196 affected checks. Initial import-path mistakes were corrected to PYTHONPATH=scripts:tests. No broad installer/full suite was run.

Saved-time planning is about31–32min for two prior-speed GLM requests, about7min for four loads and about1.5min common Q (may overlap).40–55min is a planning range, not proof at the new contexts/layouts;4500s remains a hard campaign budget and7200s per-request ceiling is clamped to remaining time. Allocation, model correctness, CPU comparison and restoration remain live proof obligations.

The task-local handoff contains the exact commit, source bundle, clock-free arm/receipt, INITIAL progress, incoming/control files, immutable private fixture/count/proof copies, protected-key metadata only, non-authorizing GO template, complete read-chain and non-network exact-package check. Root source/profile/arm review and fresh writer ownership precede any RUN. See docs/concurrent-480k-cpu.md.

# Root GO — owned backend network containment

Contain the observed wildcard distributed-store listener before API exposure. Prefer a narrow dedicated internal Docker bridge for the owned image backend, publishing ONLY127.0.0.1:30007 on the host. Native HTTP may bind0.0.0.0 inside the isolated namespace; internal30008/30009/30010 stay unpublished. A simpler documented runtime bind flag is acceptable if already known; do not spend time on upstream research.

Root authorizes this routine owned backend network/helper update. Do not change text networks, patch PyTorch, or add ad hoc global firewall rules. Preserve the current baseline generation and evidence; no need to kill current completed model progress unless exposure cannot be contained. Record actual host listeners and external refusal evidence after containment. This deployment containment adjustment is separate from the single reviewed SGLang UUID repair.

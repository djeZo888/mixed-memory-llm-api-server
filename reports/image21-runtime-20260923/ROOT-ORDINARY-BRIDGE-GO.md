# Root acceptance — dedicated ordinary bridge

Root reviewed the concrete internal-bridge publication limitation. A dedicated ordinary bridge with ONLY host127.0.0.1:30007 publication is accepted. Preserve exact task-owned network identity/member checks and unpublished30008/30009/30010 coordination ports; no shared default or text network.

Record the internal-bridge limitation and actual ordinary-network mode. Proceed with the required warm and single edit. No more network variants or additional network research.

For final acceptance, bind/publication readback and the requested four-port external refusal evidence must be captured while the contained backend is actually running/ready. A refusal captured before the new container starts is diagnostic only. Reuse the same bounded TCP-connect-only check, with no HTTP/inference request or broad scan.

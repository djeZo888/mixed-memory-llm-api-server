# Use the exact reviewed helper preset

Root's later exact deployment GO explicitly selects --example lake-bled from the reviewed helper; that supersedes the coordinator's earlier draft summer prompt. Use the preset as supplied, seed42 and fhd-acceptance-lake-bled.png, without --prompt override. Record its actual full prompt in the acceptance receipt so visual review is against the actual request.

The original root task said an Alpine/Bled prompt was convenient, not a strict summer-only requirement. This is routine prompt selection within the already approved one-call workflow, not another approval gate. Continue the sole acceptance after readiness/output-path checks.

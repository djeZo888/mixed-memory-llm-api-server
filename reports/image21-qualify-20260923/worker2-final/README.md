# Final independent GET evidence

The four authenticated GETs completed at 04:31:40 UTC, all HTTP 200: live, ready, models and capabilities. There were zero POSTs, retries or VM mutations. The service was ready, admitting and idle with exactly six opaque generation profiles and no edits.

Receipts are byte-exact. Their pending Worker1 artifact reconciliation and older RESULT.json not-tested list are historical: FINAL-GET-RESULT.json supersedes the earlier final-GET status. Local Worker1 comparison now passes in [FINAL-WORKER2-PROJECTION.json](../FINAL-WORKER2-PROJECTION.json). Independent Worker2 artifact reconciliation remains for root/Worker2; it is not claimed here. GETs do not establish source commit or post-recovery container ID.

# L2VM session — 2026-09-15

Base: `5e713441d9ea164b81860ee795c5ef35972ee8e3`; branch `milestone/l2vm-existing-host-staging`.
Execution: mac-worker1, SSH alias `ai-vm` only. Installer STOPPED.

Root approval authorizes exact registration, four nonrecursive metadata corrections after D3B release, historical instance binding, and protected control closure/config/key/unit staging. Control must remain stopped/disabled; GLM and both intent/state files must remain unchanged. V3 owns generation. No API requests or lifecycle model operations.

Initial coordination gate: HOLD `/etc/local-ai-server` creation and four top-level metadata corrections until explicit durable D3B build-running/direct-child release. Independent read-only inspection and local preparation proceed. The current root/user instruction supersedes the old activation plan's suggested D1/32K compatibility option: no compatibility profiles will be installed.

Canonical guards are the exact protected D3 paths and SHA256s from supplied `VM-GUARDS.json`, independently matched on VM with protected root-owned ancestry. Initial SSH identity is host `llmserver`, ordinary user 1000 with noninteractive sudo. `/etc/llm-server` now exists root:root0755 and belongs to N1VM; reuse protected parent without changing its metadata. This is an expected owner update from the earlier absent snapshot.

Initial pending work: fresh bounded inventory, concrete preapply payload/manifest/diff, coordination release, guarded apply and final unchanged checks. Q38VD source/auth/runtime and final measured patched GLM inputs remain separate gates.

03:00Z coordination released the four roots and registry parent after D3B's one build attempt failed before compilation. Its terminal ownership release explicitly supersedes build-running wording; no retry is active. L2VM may proceed after exact-current checks. D3B's owned run remains user:ai2770/inode44302940 and must be preserved.

Initial rootguard invocation used `--report /dev/stdout`. Its implementation captures `write_report_body` stdout into a conclusion variable, so that report target contaminated the string comparison and returned exit1 without emitting the body. This is an invocation failure, not established storage STOP evidence. Corrected read-only invocation retains original SSH stdout on FD3 and uses `/proc/self/fd/3`; inspect its actual STOP entries before any apply. No guard source/threshold/environment identity override is used. Initial incomplete report retained.

Final: registration/import/stopped staging PASS; rootguard and installed registered guard PASS. Initial positional Docker Mounts comparison failed after staging because inspect order changed; normalized exact tuples and all required container/state fields passed follow-up. Q38 protected81 receipt/link published with actual reviewed contract PASS, no runtime/auth promotion. Registration-ready delivered/acknowledged before final bookkeeping. Logs handoff prose corrected to numeric uid0:gid1000/mode2755/inode47972353; no metadata correction needed. See final milestone report for complete evidence and remaining owner gates.

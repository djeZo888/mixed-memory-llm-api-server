# Q38R — Qwen3.8-27B FP8 evaluation

**2026-09-15 · Mac-Worker2 · GO for a later bounded evaluation; NO-GO for download, activation, replacement or a speed claim now.** GLM remains first; F1S/L1 priority, authentication and one-active-model policy remain unchanged. This research authorizes no third model download. Base `c3fe68cea75c2e581f0fac56c8054cc4a10059d5`; branch `milestone/q38r-qwen38-evaluation`.

## Exact checkpoint and evidence

Official **`Qwen/Qwen3.8-27B-FP8@017b9c7af6b5689d5dd426a76e0bc077eb5ca20a`**, Apache-2.0; retain its license/notices. Current metadata and immutable revision/tree agree: **81 artifacts, 66 weight files, 30,890,049,597 bytes total; weights 30,866,866,928 bytes = 28.747 GiB**. All 15 small assets were fetched and checked against Git-blob or LFS identities; weight SHA256s are publisher metadata, **not downloaded-file verification**. The [compact source/weight manifest](q38r-source-weight-manifest.json) records every path, byte count, SHA identity, provenance and future settings. [Pinned metadata](https://huggingface.co/api/models/Qwen/Qwen3.8-27B-FP8/revision/017b9c7af6b5689d5dd426a76e0bc077eb5ca20a?blobs=true), [license](https://huggingface.co/Qwen/Qwen3.8-27B-FP8/blob/017b9c7af6b5689d5dd426a76e0bc077eb5ca20a/LICENSE).

The actual class is **`Qwen3_5ForConditionalGeneration`**, `model_type=qwen3_5`, text `qwen3_5_text`; `language_model_only=false`. Config gives dense FFNs, hidden 5120/intermediate 17408, 64 layers: **48 Gated DeltaNet + 16 full attention**; GQA 24 query/4 KV heads, head dimension 256. Linear heads: 16 QK/48 V, dimensions 128, convolution width 4. Native maximum **262,144**; the advertised **1,000,000** requires extension and is separate below. FP8 is dynamic-activation E4M3, 128×128 weight blocks, with 882 exclusion names. Metadata reports 24,699,207,680 FP8 and 3,082,220,272 BF16 elements: embedding/head/vision and other exclusions make “27B × one byte” inadequate. One MTP layer is included. [Exact config](https://huggingface.co/Qwen/Qwen3.8-27B-FP8/blob/017b9c7af6b5689d5dd426a76e0bc077eb5ca20a/config.json).

## Runtime: existing image is a conditional candidate

Keep image **`sha256:5027e95bf6ec536856b1b52a91d1f35ff5c564ab83e8a94758a169ff09bb8df3`**, SGLang 0.5.14 / Torch 2.11.0+cu130 / Transformers 5.8.1 / FlashInfer 0.6.12, as the first candidate. No demonstrated architecture blocker requires a new runtime, but it is **not Qwen3.8-installed-verified**.

| Support level | Finding / remaining gap |
| --- | --- |
| **Official recipe** | Current SGLang Qwen3.8 cookbook reports v0.5.19 results, including RTX PRO 6000; 1,319-question GSM8K, input 8,192/output 1,024, concurrency 1. Throughput was not remeasured in that sweep. Its install instructions use latest/prerelease; **earliest stable compatible version is unestablished**. v0.5.19 is a documented measured stable version, not evidence for 128K/256K or our image. |
| **Exact-release code only** | v0.5.14 = `49e384ce9d304648e9959666ecb8ce8cd98d0deb`: class registration, `qwen3_5` config, dense/VL weight-name mapping and safetensors loader exist. Loader skips MTP without speculation; vision can remain loaded for text requests. Hybrid attention dispatch/GDN Triton paths exist. These architecture modules are absent from supplied installed snapshots; require exact load coverage, since unknown parameters can be skipped. |
| **Installed fixture, supplied F1A** | Release hashes match F1A records for QwenNext, FP8 utilities, coder detector/parser registry and server arguments. Auto FP8 GEMM selects Triton on SM120; F1A's tiny block-FP8 linear passed both GPUs. This proves neither Qwen3.8 loading nor its attention/GDN kernels. |
| **Live Qwen3.8** | **NOT_TESTED:** weight integrity, loading, complete SM120 kernels, allocation, auth integration, generation, streamed tools, continuation and performance. |

Sources: [official cookbook at its inspected commit](https://github.com/sgl-project/sglang/blob/df6424967ab850cc53bf0321904a11a0cce099b2/docs/cookbook/autoregressive/Qwen/Qwen3.8-27B.mdx), [exact-release model](https://github.com/sgl-project/sglang/blob/49e384ce9d304648e9959666ecb8ce8cd98d0deb/python/sglang/srt/models/qwen3_5.py), [F1A contract](f1a-sglang-contract.md). Source hashes/support levels are enumerated in the manifest. Config's `transformers_version=5.8.0.dev0` records producer provenance, not a proven minimum. Installed FlashInfer 0.6.12 also misses the recipe's MTP version floor (>0.6.15.post1); speculation stays disabled.

**Concrete API gap:** use `qwen3_coder` tools plus `qwen3` reasoning parser and the pinned checkpoint template. It preserves `reasoning_content` by default and emits XML-style function calls. v0.5.14's top-level effort enum rejects `xhigh`; accepted `high`/`max` conflict with this template. Initially **omit effort** (template defaults xhigh), or use **low/medium**; disable thinking with `chat_template_kwargs.enable_thinking=false`. Avoid an untested kwargs bypass. Exact code handles streamed tool fragments and `continue_final_message`, but template rendering, reasoning retention, call IDs/JSON and tool-result continuation require fixtures and live proof. [Template](https://huggingface.co/Qwen/Qwen3.8-27B-FP8/blob/017b9c7af6b5689d5dd426a76e0bc077eb5ca20a/chat_template.jinja), [protocol](https://github.com/sgl-project/sglang/blob/49e384ce9d304648e9959666ecb8ce8cd98d0deb/python/sglang/srt/entrypoints/openai/protocol.py), [chat handling](https://github.com/sgl-project/sglang/blob/49e384ce9d304648e9959666ecb8ce8cd98d0deb/python/sglang/srt/entrypoints/openai/serving_chat.py).

## Capacity plan: actual total tokens, one sequence

Established hardware: two 97,887 MiB GPUs (**95.593 GiB each**, 191.19 aggregate), SM120, no NVLink; 881.78 GiB RAM, Threadripper 9985WX. Claimed eight-channel ~400 GB/s RAM bandwidth is unmeasured. VRAM pools are separate.

**BF16 KV** = `tokens × 16 layers × 2(K,V) × 4 KV heads × 256 × 2 bytes` = **65,536 bytes/token**. FP8 weights do not imply FP8 KV. One linear-state slot = `48 × [(48×128×128×4) + ((2×16×128+48×128)×3×2)]` = **153,944,064 bytes**: FP32 recurrent state, BF16 convolution. TP2 divides both evenly. Release pool allocates `size+1`; a capped one-slot pool therefore costs **0.286743 GiB aggregate**, including its sentinel. [State geometry/dtypes](https://github.com/sgl-project/sglang/blob/49e384ce9d304648e9959666ecb8ce8cd98d0deb/python/sglang/srt/configs/mamba_utils.py), [pool](https://github.com/sgl-project/sglang/blob/49e384ce9d304648e9959666ecb8ce8cd98d0deb/python/sglang/srt/mem_cache/memory_pool.py).

Planning budgets below include **8 GiB workspace + 8 GiB safety reserve per GPU**. Weight allowance: 32 GiB TP1, 18 GiB/GPU TP2; these exceed serialized 28.747/ideal-half 14.374 GiB, allowing padding/layout/replication. Even duplicating the entire outside shard gives a 17.171 GiB/GPU weight proxy. These allowances are assumptions, not measured upper bounds.

| Total context | KV aggregate | TP1 total / nominal free | TP2 per GPU / aggregate |
| --- | ---: | ---: | ---: |
| 131,072 (128K) | 8 GiB | **56.29 / 39.31 GiB** | **38.14 / 76.29 GiB** |
| 262,144 (256K) | 16 GiB | **64.29 / 31.31 GiB** | **42.14 / 84.29 GiB** |

Bounds require one running request, `--max-mamba-cache-size 1`, disabled radix cache/speculation/graphs, BF16 KV and `--max-total-tokens` equal to the context cap. Otherwise preallocated pools can exceed this arithmetic. Budget includes all serialized vision/MTP weights conservatively; excludes additional sequences, retained prefix states, multimodal activations, speculative buffers and transient load copies. CUDA contexts, allocator fragmentation, attention/collective buffers and chunked-prefill activations must fit the assumed workspace/reserve; verify actual peaks. Disabling radix means repeated tool turns can re-prefill history and is an initial acceptance setting.

**Optional extension only:** 1,000,000 tokens needs 61.035 GiB BF16 KV: TP1 plan **109.32 GiB (NO-GO)**; TP2 **64.66/GPU, 129.32 aggregate (capacity arithmetic only)**. Native config has no extension override; approved RoPE/YaRN configuration, quality and latency validation are missing. Do not substitute 1,048,576 for the card's 1,000,000 or launch at an advertised maximum.

## Comparison and one recommended future path

Coder-Next FP8 is 80B/3B-active MoE, 74.861 GiB weights, non-thinking and coding-focused. Its 12 full-attention layers × 2 KV heads × 256 BF16 cache need **3/6 GiB at 128K/256K**; Qwen3.8 needs **2.67× more KV** despite smaller weights. Dense 27B compute versus ~3B-active MoE does **not** establish faster prefill/decode; thinking adds tokens. Single GPU avoids PCIe collectives; TP2 offers more headroom with communication costs. All-GPU fit proves no throughput advantage. [Pinned comparator config/card](https://huggingface.co/Qwen/Qwen3-Coder-Next-FP8/tree/da6e2ed27304dd39abadd9c82ef50e8de67bdd4c).

Official quality scores are not a common deployment benchmark: Qwen3.8 SWE-bench Pro uses Claude Code, temperature 1/top-p .95 and 256K, with corrected tasks/re-evaluated baselines; QwenSWEBench is in-house avg@3, eight-hour timeout/32,768 output. Its FP8 near-parity claim lacks our hardware validation; Coder-Next explicitly reports original BF16 results. Infer neither tool-quality ranking nor FP8 speed from those tables. [Qwen3.8 card](https://huggingface.co/Qwen/Qwen3.8-27B-FP8/blob/017b9c7af6b5689d5dd426a76e0bc077eb5ca20a/README.md).

**Best path after GLM/F1S/L1:** retain this image; verify missing installed module hashes/imports and exact template/parser fixtures first. Add a bounded declarative profile and reviewed F1S allowlist changes for new identity, TP1, context, reasoning/backend/cache flags. F1S's current Coder-Next-only alias/TP2/32K launcher cannot be reused unchanged. Preserve file auth with keys outside ServerArgs/argv/env, authenticated warmup, WebSocket denial, one tokenizer, loopback/SSH forwarding, shared lifecycle lock and GLM rollback. Run the actual-image sentinel gate before real keys. Escalate to separately reviewed pinned v0.5.19 runtime only if concrete compatibility tests fail; no upgrade now.

Then separately authorize exact `/data` acquisition and checksums; run mount/root guards before/after heavy work. First validate **128K**, then **256K**: TP1, one request, safetensors/FP8, FlashInfer attention, Triton GDN/GEMM, chunked prefill 4096; no remote code or speculation. Reserve **at least 8K tokens inside each cap** for reasoning/tool results/output; count system/tools/template/history too. Prove nonstream/SSE, fragmented calls, continuation/cancel/retry and a real client read→edit→test loop. Record actual input/output tokens, time to first token, prefill/decode tokens/s, end-to-end task latency, peak/per-GPU memory and safe rejection beyond the cap. Repeated full-attention prefill can grow roughly quadratically; large context is not automatically fast. TP2 is a measured fallback, not the starting choice.

## Checks and handoff

**PASS:** read AGENTS/current coordination/R2/F1A and sibling F1S contracts; exact metadata/tree/index-set and 15 asset hash checks; independent source and memory audits; JSON/arithmetic/local-link checks and packaging gates recorded in taskroot `final.md`. **Warnings:** missing installed architecture files; newer official recipe; effort mismatch; unmeasured workspace/performance. Real weights/KV/inference **NOT_TESTED**. No SSH, weight download, build/install, service action or implementation edit occurred. Verification commands: `python3 -m json.tool reports/q38r-source-weight-manifest.json` and `git diff --check`; manifest reproduction steps define metadata/hash rechecks and separately gated future acquisition. Only this report and its manifest belong to the commit. Next action: root reviews research; F1S/L1 and GLM acceptance continue unchanged.

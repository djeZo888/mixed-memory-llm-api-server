# H004 GPU diagnosis and bounded bandwidth — 2026-09-24

**Three healthy GPUs completed the accepted transfer measurement; the fourth GPU still fails initialization.** Both 480,000-token text Qwens and the Ada image model remained resident. This report derives from retained task evidence; publication involved no further VM or host contact. The [machine-readable table](results.json) includes all 30 measured samples.

| Guest PCI / full UUID | Exact NVIDIA model | VRAM MiB | Idle/current link | Maximum link: endpoint / NVML | Active observed link | Median H2D / D2H GB/s | GPU °C min–max | Memory temperature |
|---|---|---:|---|---|---|---:|---:|---|
| `0000:01:00.0` / `GPU-88058d9d-08e5-cb1e-a77a-04cbc1488237` | NVIDIA RTX PRO 6000 Blackwell Workstation Edition | 97887 | Gen1 x16 | Gen5 x16 / Gen5 x16 | Gen5 x16 | **57.00 / 56.50** | 33–35 | Unavailable |
| `0000:02:00.0` / `GPU-69acfa26-8b60-61b5-702d-aee252c163cc` | NVIDIA RTX PRO 6000 Blackwell Workstation Edition | 97887 | Gen1 x16 | Gen5 x16 / Gen5 x16 | Gen5 x16 | **57.00 / 56.50** | 34–36 | Unavailable |
| `0000:03:00.0` / `GPU-5d895991-b794-2b4c-b9c4-5f1b668afd23` | NVIDIA RTX 6000 Ada Generation | 49140 | Gen1 x4 | Gen4 x16 / Gen3 x16 | Gen3 x4 | **2.61 / 2.89** | 32–38 | Unavailable |
| `0000:04:00.0` / unavailable | NVIDIA RTX PRO 6000 Blackwell Server Edition | Unavailable | Gen3 x4, failed initialization | Gen5 x16 / unavailable | NOT_TESTED | NOT_TESTED | Unavailable | Unavailable |

Idle/current denotes the retained no-transfer snapshot; fourth-card Gen3 x4 is not an active-load measurement. VRAM is NVML-reported MiB, not BAR aperture size. Temperatures are min/max over the accepted short measurement window. Decimal GB/s means 10^9 bytes/s; measured precision is two decimals.

Retained NVIDIA **nvbandwidth v0.10.0**, source pin `82fc4e8c6afa0babb8687793678f615b3b8d793e`, used **256 MiB (268,435,456-byte) pinned buffers**, copy-engine H2D/D2H, one excluded warmup per direction per healthy UUID, then five samples per direction. Full-UUID CUDA selection exposed only the intended healthy card; native verification remained enabled and all six processes exited 0. CPU affinity was 16–23. No completed valid case was repeated. This bounded buffer size does not establish peak platform bandwidth.

Persistent healthy NVML handles collected **158 telemetry frames**, each covering all three healthy cards, from **12:58:07.421602 to 12:58:38.834301 UTC**. Nominal interval was 200 ms; observed intervals were 135.957–264.089 ms, median 200.070 ms. Memory sensors were unavailable in retained discovery. The core stop ceiling was 85°C, below retained slowdown-minus-5°C and other known operating limits. New/unknown GPU errors, healthy-card errors, hardware/thermal slowdown, lost/stale telemetry and driver hangs remained stop conditions; none occurred.

The exact known **GPU3 / `0000:04:00.0`** WPR2/GSP/RmInitAdapter signature, including bad register `0xbadf4100`, was counted explicitly: **13 occurrences, 78 lines** (one six-line sequence during NVML startup and two during each of six native process startups). No healthy-card or unknown GPU error was recorded. This allowance does **not** imply fourth-card recovery. Two earlier conservative monitoring-attribution attempts stopped on that same known fault; they are superseded, not evidence of healthy CUDA failure. The partial earlier 57.00 GB/s H2D warmup is excluded from accepted samples.

Installed **NVIDIA 595.84 open driver** supports exact device/subsystem `2BB5/204E`, confirmed by retained installed metadata and the [official supported-device list](https://download.nvidia.com/XFree86/Linux-x86_64/595.84/README/supportedchips.html). The earliest observed current-boot failure is FSP task `0x1`, command `0x14`, error `0x6`, followed by inability to boot GSP RM; WPR2 failures occur later. This locates the initial observed failure, but does not establish its cause. The user confirms full physical-host poweroff during installation. Neither a warm-reboot explanation nor a reset/coldboot fix is established; missing early VFIO binding of `10de:2bb5` remains a hypothesis. No blind driver upgrade is justified by this evidence.

Read-only guest evidence exposes PCI `FLReset+` and sysfs `reset_method` = `flr bus`. The target is alone behind virtual port `00:1c.3`, and no open target `/dev/nvidia3` descriptors were found at the snapshot. NVIDIA remained bound; guest evidence establishes neither physical reset isolation nor kernel quiescence. NVIDIA GPU reset, Linux function-level reset and PCI bus reset are different operations; a sibling-affecting reset is outside scope. **No safe supported isolated guest reset path was established, and no reset was executed.**

Container identities, PIDs, start times, images, restart counts and GPU assignments were unchanged before/after the completed run. The current registered-storage/root guards passed, with the existing low-root-space warning; the canonical lifecycle lease was released and no nvbandwidth process remained. No extra inference, fourth-card transfer load, service/driver change or Proxmox contact occurred. External client activity was not controlled. Results do not establish sustained thermal qualification or inference/context capacity. Fourth-card bandwidth is **NOT_TESTED**; new-card image-capacity testing is **USER_AUTHORIZED but BLOCKED / NOT_TESTED** because the fourth GPU cannot initialize and lacks telemetry.

**Next action:** root reviews the user's read-only Proxmox host and riser/dock/power evidence, then coordinates the next safe image-capacity window once the card is healthy. Benchmark permission is already granted; technical readiness remains the blocker. No further reset testing is part of this report. Raw traces and private artifacts remain outside Git. Source base: `b2f6580cc5283479f28854b37e3200117610ddd0`.

# D3T revised concrete runner contract (coordination Revision4)

Scope trim ACK. SOURCE plus bounded Revision3 native non-generation inspection;
no generation, reload/build, installer, VM mutation or live acceptance in D3T.

Ownership: `scripts/d3t/probe.py` task-specific CLI/checkpoint; `accounting.py`
actual installed /apply-template + /tokenize and A1 parsing/local read tool;
`guards.py` concrete existing-VM SSH resource/identity checks; `profiles.py`
immutable NOT_TESTED generation source. These are narrow helpers for ONE trial,
not a lifecycle/authorization/receipt framework. Tests only `tests/d3t/`.

Revision2 Q38B ACK permits only llama bodies of Manager.validate_deployment and
Manager.launch_command, exact diff published BEFORE edits in
`../manager-seam-plan.diff`; imports/Q38 dispatch/control/installer unchanged.

CLI: init with baseline/candidate/native exact container+image IDs after separate
live review; prepare exact cold/warm/tool/continuation body; account invokes
actual native non-generation endpoints (no imported receipt); start detaches one
bounded request; status resumes observation without dispatch; cancel closes its
own socket only. Owner checks status<=60s. Private0700 run outside Git,0600
atomic checkpoints/body/raw/token files; key read in memory, never argv/logs/Git.
No retries; ambiguous dispatched state needs owner reconciliation, later stages
remain PENDING/NOT_TESTED. No tunnel/PID kills, fallback or reload by the runner.

Compare 32K baseline/candidate same body fixture, empty-cache then warm,
low effort/temperature0 and256/256/512/512 output caps;600s/request and2400s
combined generation cap. Native phase is ONE1048576 container, occupancy targets
65536/131072/262144/524288/1048576,8192 reserve, three requests128/256/256,
2/4/8/12/24h stage caps. Actual native template/tools/special-token accounting,
stable prefixes and native continuation IDs are checkpointed. No shift/truncation/
RoPE scaling. Functional pass needs early/middle/late content, actual worker-local
A1 read_file(calc.py), continuation and exposed useful cache reuse. Missing backend
counts remain null. Failed stage never proves blanket incapacity.

Reuse exact current-VM guards: mounts/UUID,root STOP<4GiB,no swap/CUDA/OOM/storage,
RSS/PSS/MemAvailable and each GPU free>=16GiB, bounded1Hz samples (not instantaneous
peaks). Direct installed source/log inspection establishes native fused_lid/indexer,
FA and allocated workspace observability. If insufficient, publish ONE tiny
startup diagnostic patch plan for D3P/root combined build; do not add a framework.
Before occupancy require actual native capacity/selected path/allocation evidence.

D1 image is old all-CPU32K baseline only. NOT_TESTED N76 profile sources preserve
F16/split1,1/one slot/defaults; no candidate image invented. D3P supplies measured
strict-alias patched image, later deployment binds it. All-CPU native is the only
headroom fallback, selected separately outside client ownership. No push.
Final handoff: report, source commit with CodexAIagent author+committer,
secret/diff scan, ../D3T.bundle + ../handoff.md; stop after handoff.

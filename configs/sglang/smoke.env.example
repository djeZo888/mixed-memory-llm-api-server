# M8A placeholder only. Do not commit real secrets.
# Proposed pinned image after M8B remediation review: lmsysorg/sglang:v0.5.14-cu130
SGLANG_IMAGE_TAG=
SGLANG_MODEL_PROFILE=qwen3-0.6b-smoke
SGLANG_RUNTIME_PROFILE=sglang
SGLANG_HOST_BIND=127.0.0.1
SGLANG_PORT=30000
SGLANG_MODEL_PATH=/data/models/qwen3-0.6b-smoke
SGLANG_LOG_DIR=/data/logs/sglang-smoke

# Qwen/Qwen3-0.6B should not require HF_TOKEN if public, but tokens must be stored outside git if ever needed.

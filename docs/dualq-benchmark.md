# Closed dual-Qwen benchmark source handoff

`DUALQ-BENCH-PREP-20260921`, native session
`01a0c17f-962e-70f1-8411-f65ffa187d3f`, starts at frozen production candidate
`f130ec46ebd9a27319d84e0d372746e0489a9972`. This is source/offline preparation.
No VM contact, keeper action, inference, deployment, push or merge is performed.
Production/runtime/profile/fixture/registered-guard/lease and rollback source
remains byte-identical to that candidate. Source tests are synthetic evidence.

The actual entrypoint remains `scripts/bench/run-gpu-split.py`; its closed
`dualq72-480k` dispatch uses `benchmark.dualq_run`, `DualQRun`, the existing
`DiagnosticSSHHost`, remote `scripts/bench/benchmark-host.py`, `LinuxHost`, and
`CampaignOwner`. The installed canonical lifecycle lease and registered writers
remain authoritative. No router, daemon, placement framework, owner adoption or
production acceptance bypass is added.

```sh
python3 scripts/bench/run-gpu-split.py arm --state /absolute/private/package \
  --scope dualq72-480k --session-id PREP_SESSION
# Future fresh RUN only after exact source/arm GO and required receipts:
python3 scripts/bench/run-gpu-split.py run --state /absolute/private/package \
  --go /absolute/private/GO.json --session-id FRESH_RUN_SESSION
```

The arm binds the clean source commit, complete source hash inventory, exact
predecessor/container/release identity, copied scope/production receipt, existing
protected key metadata, two manifests, trial plan and independent request clock.
Keys remain in their existing protected files outside the package and Git.
The final task handoff supplies exact source/bundle/arm/package hashes. Its
`GO.template.json` is `NOT_AUTHORIZED`; no actual-image receipt is preaccepted.

The source-only metadata FIX freezes campaign `benchrun-dualq72c1-20260921`.
The failed `benchrun-dualq72-20260921` ledger and source955ec8 remain preserved:
that corrected run was canonically RESTORED with zero warmups/measurements.
This fixed campaign needs a fresh exact-source/arm root GO; it cannot resume or
reset the old ledger. Retained actual AUTH receipts bind only the future GO.

Q0/GPU0 and Q1/GPU1 both use the actual Qwen family, pinned FP8 image/weights,
BF16 KV, YaRN4, chunk2048 and configured480000. Each manifest hashes its frozen
production480000 profile and compares native argv with the production pair
launcher, changing only temporary benchmark ports/aliases. GPU UUID, Q8 shared
CPUs0-7, 32GiB cap/no swap and15% sampled required-working-set margin are checked.
Two Q8 masks have union8 and exclusive0 on the72CPU/eight-node guest.

The finite sequence loads each once, native-counts and runs one discarded
32-output warmup each, then native-counts the final measured512-output bodies.
Both current admissions complete before the HTTP dispatch barrier. Exactly one
measured retrieval per role is sealed durably; no reload, baseline, repeated pair,
tuning, automatic retry, filler or output padding. The familiar deterministic
archive/scorer retains early/middle/late retrieval and strict/semantic results.

Prefixes are frozen before load: `fresh-dualq-20260921-q0` and
`fresh-dualq-20260921-q1`. The nominal input target is deliberately479423,
64 below the historical479487. Removing two archive rows (12148 versus12150)
provides approximately79 tokens of structural room for the fixed fresh prefix;
this is not a claimed native count. The single final native count must be within
479360..479487, have the pinned Qwen template, and fit512 output within the actual
request bound479999. There is no exact479487 requirement or live fitter. Report
actual input and the deliberate reduction separately from configured480000.

Before every count/warmup/measurement admission, refresh actual GPU UUID,
container/profile/auth identity, actual native pool480000/input479994, strict
context480000/TP1 and current resource proof. `max_req_len` is optional, as in the
production gate; when present it must be the integer479999. Absence stays null
in proof. Every fixed input+output budget fits the known pinned pool-1 bound479999.
Missing required observations block new admission without
cancelling a healthy peer; numerical unsafe observations latch and cancel.
Every admitted inference receives7200s starting at HTTP dispatch. The reused
preparation/admission clock never clips an admitted request. Four to six minutes
is an expectation only. Report actual elapsed, overlap and each role's tail.

The existing low-overhead cgroup/process/guest/GPU/RAM collector and phase
summary retain sampled peaks, raw charges and required estimates separately.
Qwen-native cache/timing fields remain null when unavailable. Client rates and
phase boundaries explicitly say client event-arrival proxies. Configured versus
occupied context and512 cap versus actual output are separate report fields.
No profiler, driver change, model download or extra measured requests are used.

Both HTTP lanes drain before restoration, including a failed peer. Timeout,
cancel or uncertain transport does not claim Qwen native idle: its registration
remains until canonical stop proves exact owned processes/cgroup gone. Deferred
signals during successful admission register `NOT_DISPATCHED` cleanup. Captured
original state must be STOPPED/manual before ownership; no running adoption or
production stop/start is used. Cleanup returns to that original state with
canonical lease release and independent worker verification.

The historical predecessor is sourceb69bd5c2a34adb462ee87333e2a238bd1c05436d,
session01a0c0b6-2901-7943-acbd-dd02d87b7e29, keeper71966/SSH71972,
remoteowner1292520/startticks2770655. Exact full container/image IDs and old
canonical RELEASE entrypoint are in the private predecessor artifact. A separate
root action updated saved old-task restoration files during PREP; these local
files are not fresh live proof. Root must bind the reviewed canonical release
receipt plus refresh STOPPED/manual/lease-free/zero-owned-resource/listener proof.
Do not re-release an already verified restored predecessor.

The [actual-image auth proposal](dualq-benchmark-auth-stage.md) uses the existing
base/Q0/Q1 fixtures after old release under the canonical lease, before Q/Q loads.
Its current CPU contract is thread limits only (no Docker affinity/quota), stated
explicitly for root review. No GPU, model weights or inference is used there.
Only actual completed fixture receipts can satisfy the protected auth gate.
Benchmark result correctness/performance is separately reviewed before production
acceptance/activation, which is outside this finite owner.

# REAL72 held-job retention correction and sealed warm-only PREP

Source-only base `d257c2a6531eb29f47e210c98d64341d23a373ee`; root design decision is in task `POSTRESTART72-RETENTIONFIX-20260920/ROOT-DESIGN-GO.json`. No VM action is part of PREP.

RUN4 completed one G4K retrieval PASS with3538 input/256 output/cache0 and OUTPUT_LIMIT:626.918232s total,67.395964 input tok/s,0.444114 native output tok/s. The pair initially held. A later hold checkpoint failed through `host.gate -> host.jobs -> require(not raw, pending_systemd_jobs)` at the pinned source; original diagnostic did not retain the job identity. Canonical restoration stopped both and restored STOPPED/manual. This failed retention and all historical results remain unchanged. No CPU/memory fault has been established.

For REAL72 idle `warm_hold` only, retain operation-aware systemd snapshot evidence. Provably unrelated jobs may continue; incomplete or ambiguous proof yields guarded REVIEW_REQUIRED and refuses new admission without declaring danger or safe HELD. Actual owned lifecycle/control/container-runtime, storage/mount/device and shutdown hazards retain the existing protection. Ordinary lifecycle/load/request/restore job gates remain strict. Current PID-bound lease, original snapshot, exact pair, native480000 allocation, authentication, mounts, resources and drain proofs remain required.

The fixed campaign `benchrun-p72wh-20260920` seals `warm_only` mode in the source arm and GO. It runs the same two480000 loads, then the existing discarded G~4K32 and Q3251x32 warmups, then retained hold. It dispatches no initial G4K measurement, long pair or optional case. GLM CPU0-71/Qwen shared CPU0-7/Mems0-7, images, precision,640/32GiB caps and sampled required-estimate×1.15 policy are unchanged. The independent preparation clock and full7200 request budget are unchanged. The original measured campaign remains separately bound.

A later preset request remains possible only through the existing fresh source/session/hold/manifest/body/fixture-bound followup GO; the initial measurement clock may still be unstarted. No automatic followup, warm adoption, daemon, activation or production migration is introduced. Fresh root review of exact source and raw arm bytes plus a fresh RUN session precedes live use. Offline tests and package checks establish source behavior only; live retained residency is unproven by this correction.

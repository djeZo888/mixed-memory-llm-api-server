# Resumed first-stage work — 2026-09-17

The user explicitly resumed implementation through completion of ai-vm API serving. The latest clarification accepts GLM using both GPUs and restores **one active model at a time**, selected through the API before inference. Keep exactly GLM5.3 UD-Q4_K_XL and Qwen3.8-27B FP8; maximize practical supported contexts for each. No co-residency redesign, frontend, extra models, or installer work.

Mac-Orchestrator coordinates/reviews/synchronizes only. Fresh bounded remote Codex CLI sessions on both workers implement/build/test in isolated copies. Worker1 owns VM mutation/request coordination; Worker2 source and independent API acceptance. Use existing guards/leases/keys, preserve rollback and avoid new framework/report loops. Read older handoffs as dated evidence, not active STOP instructions.

Current starting head:3ea7b14fd60c364bb894d44b76790d99ea3fd80d. Initial parallel tasks: Worker1 RESUME1 refresh VM facts and retained GLM health; Worker2 Q38FIN resolve exact launch_environment_invalid and implement already-planned production Qwen core containment. Their outputs determine next native model trial and final existing single-backend API composition.

See [paused handoff](2026-09-17-handoff.md) for the preserved baseline and [progress report](2026-09-17-progress-report.md) for historical completed/remaining work. Subsequent dated task results override that baseline.

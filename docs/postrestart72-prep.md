# Actual72 closed 480K source preparation

This task starts from published `4db2c7b888ac68e94f62a0275398a7d18c966af3`; unreviewed resident96 WIP is excluded. Scope `postrestart72-480k`, campaign `benchrun-p72-20260920`. PREP is offline source plus authorized read-only Worker1 guest inspection. Root source/arm review and a fresh RUN session are required before staging, loads or requests. No installer, frontend, production activation, model selection, Proxmox/Worker2 contact or push is included.

Both models configure480000 tokens with existing exact weight/image/UUID/launch pins: GLM UD-Q4_K_XL/N76/F16/GPU0,72 threads and batch threads, guest CPU0-71; Qwen FP8/BF16/YaRN4/TP1/static0.8/chunk2048/GPU1, allowed CPU0-7. Q8 shares72 guest CPUs with GLM; it is affinity capacity, not eight runtime threads or80 available CPUs. Both allow all guest memory nodes0-7. Caps640/32GiB, no swap and explicit sampled required working-set ESTIMATE×1.15 remain. Raw cache/current/peak remain separate. Existing host available688GiB admission, remaining-cap obligations,16GiB OS/GPU reserves, Qwen10% GPU comparison, OOM, sustained owned swap, protected storage/identity/lease guards remain.

The guest baseline proves online0-71, eight nodes with56 online2GiB blocks each (112GiB/node),896GiB total, both exact GPUs, and captured STOPPED/manual original intent. Kernel usable memory differs from configured memory. Supplied QEMU configuration maps guest nodes to preferred host nodes6,0,2,4,1,7,3,5 and permits host logical CPUs0-63,72-79; it proves neither exclusive physical pinning nor full RAM bandwidth. Current free RAM/VRAM and root headroom are volatile. Preserved private ingress systemd sockets30002/30004 are distinct from absent native backends/benchmark listeners. Original state changed only its shutdown timestamp; all stopped/manual intent fields remain. Original shutdown receipt and protected credentials are preserved.

Exactly two loads, then two discarded32-output warmups after both allocations are ready. GLM warmup uses the historical approximately4K logical archive with a fresh prefix; Qwen uses80records, approximately3251input. Warmup speed is never a gate. CPU proof requires actual72 counters and same-process/cgroup deltas; unavailable evidence stays unavailable. Quiescent CPU/Mems/NUMA proof is outside timed requests. Native Q proof resolves flat/nested configuration, rejects conflicts, and requires actual pool480000/input479994 independently of configured arguments.

Exactly three measured requests: historical short GLM108-record fixture (saved3546input/145output), then conditional concurrent G65008 and frozen Q479487. All measured output caps256. GLM keeps temp1/seed1729/low/schema; all fixtures use fresh prefixes and native count of final bytes, cached0 for GLM. No refitting, Q256 extra,112 rerun, output512, CPU-only, profiling, tuning or automatic retry. Count prefix variance remains explicit. Strict and one-fence semantic retrieval stay separate; real semantic/native/transport/resource failures stop admission and drain a healthy peer.

The short row is saved and emitted before long dispatch. Long is preauthorized only with strict correctness, uncached native count, input rate≥35tokens/s and client total≤180s. Otherwise a healthy pair enters monitored preliminary-review hold for root's bound decision. Aggregate decode<2tokens/s at≥64output is a flag; isolated slow decode never independently aborts. Historical short reference:51.750817s prompt,11.391151s decode,63.222480s total,12.641392 native(n−1)decode tokens/s. Historical pool4096 versus480000, guest/node topology, resident Qwen and single-sample variability prevent causal CPU claims. Prior480K long G941.819s/Q253.166s suggests roughly25–45minutes including two loads/warmups/counting; this is planning, not measured72 performance.

For this scope only, preparation/loading has a separately recorded7200s bound. The initial21600s measurement-admission window starts at first measured admission. Every warmup/measured HTTP dispatch receives its own full7200s. Campaign expiry refuses new admissions without clipping/cancelling an admitted request. Native count calls retain their120s bound. Restoration is outside measurement admission. Old protocol clocks and receipts are unchanged. A root-authorized one-time continuation from preliminary hold may execute only the same remaining pair inside the retained runner with a fresh source/session-bound receipt and separate admission segment; prior segment remains durable. No fresh LinuxHost begin or warm adoption exists.

Latest root/user override keeps the exact safe pair warm after completion or a review/STOP/PAUSE checkpoint. WARM_HOLD retains the existing detached RUN controller, original SSH host, PID-bound canonical lease, protected original STOPPED/manual snapshot and monitoring. No inference is accepted while held. Receipt includes actual model/container/owner/session/port/profile identities, native allocation and resource proof; Worker1 saves the receipt off-VM. Scoped STOP/PAUSE stops admissions and drains requests, then holds a fully warmed safe pair. Before such a pair exists, cleanup remains necessary. Explicit bound RELEASE restores captured STOPPED/manual; actual resource/storage/ownership failures also clean up. Hold monitoring does not expire just because measurement admission ends.

A disconnected idle held host attempts canonical restoration on EOF for this scope only. Sudden host-process death cannot guarantee automatic cleanup: durable ledger/RECOVERY_REQUIRED and explicit fresh restoration-only recovery remain authoritative. No crash adoption, unreviewed warm reconstruction, daemon/service installation, production API or arbitrary future request framework is introduced. Source/offline tests do not establish live allocation, inference, held residency or restoration acceptance.

After COMPLETE, paused or quality-review hold, one fresh-session source/profile/body/fixture-bound GO may submit exactly one additional fixed G4K/G65008/Qnear480K preset through the retained runner. It gets a separate6h admission segment and full2h dispatch budget; original results/clocks remain immutable. Invalid controls are rejected without release. Completed semantic/counter/parser/reporting failures retain a safe drained pair in REVIEW_REQUIRED hold; missing proof blocks admission without claiming current HELD. Proven danger or undrained transport uncertainty retains canonical cleanup/recovery. Cheap bounded hold checks replace timed interval collection while idle.

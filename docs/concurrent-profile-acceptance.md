# Dual-Q / optional G/Q capacity and protected acceptance

The 2026-09-21 candidate contains exactly three480K profiles and two modes:
`dual-qwen` = Qwen0/GPU0 + Qwen1/GPU1; `glm-qwen` = GLM/GPU0 + Qwen1/GPU1.
See [fixed topology](concurrent-api.md). No acceptance receipt is generated by
PREP. Historical G/Q/700160 receipts cannot authorize the new modes.

## Exact protected receipt

Use registered data root suffix
`services/llm-manager/evidence/dualq-480k.accepted.json`. Existing root-owned,
single-link mode0600 and protected ancestry/mount/anchored-read requirements
apply. Instance `concurrent_pair_acceptance` still contains exactly `path`,
canonical JSON `sha256` and `reviewed_source_commit`. No path/boolean bypass.

Required receipt header:

- integer `schema_version:2`, `kind:root-reviewed-dualq-480k`,
  `status:ACCEPTED_FOR_ACTIVATION`;
- exact instance/storage identity, reviewed40-hex source commit, complete current
  `source_identity()` hashes, GPU index/UUID inventory, `guest_total_vcpus:72`;
- nonempty evidence references and `concurrency_performance_review:ACCEPTED`;
- `host_headroom_policy` exactly version`sampled-required-working-set-15pct-v1`,
  numerator23, denominator20, basis`sampled_required_working_set_estimate_bytes`;
- `host_usable_bytes` sufficient for mode caps;
- `modes` exactly `dual-qwen` and `glm-qwen`. Each contains its own nonempty
  `evidence`, `concurrency_performance_review:ACCEPTED` and `slots` exactly
  `glm` (GPU0 placement) and `qwen` (GPU1 placement).

Every mode-slot proof binds deployment/profile SHA256, configured480000,
actual runtime image ID, model revision, CUDA0→physical UUID, guest CPU count,
mask and `guest_cpu_sharing:shared`, exact no-swap memory limits, allocation,
short inference and correctness PASS. `largest_occupied_context` is positive
and <=480000, independent of configured allocation. Earlier GLM65008 occupied
context must not become a480K retrieval claim.

Each proof supplies measured raw `host_peak_bytes` <= hard cap, positive sampled
required working-set estimate satisfying `estimate*23 <= cap*20`, GPU total and
minimum free. Raw/file/cache/peak counters do not substitute for the estimate.
Both GPUs retain16GiB; every Qwen instance also retains10% of physical GPU
capacity, including Qwen onGPU0. Qwen0 and Qwen1 each need matching pair-wrapper
SHA256 and PASS for all `qwen38.AUTH_CHECKS` from actual-image execution of their
closed slot/port/alias tuple. Fixture/source tests alone cannot supply evidence.
Updated pair fixture preserves the base auth launcher; prior fixture receipts
remain historical and cannot attest the revised fixture or new GPU0 tuple.

`check_acceptance` validates the whole protected receipt, then returns the exact
selected-mode slot projection to existing consumers. It never reinterprets G/Q
proofs as Q/Q. Current mode admission derives physical placement from trusted
resident deployment IDs, not model names or saved readiness. Missing either
mode/proof fails closed; exact root review precedes receipt publication.

## Fresh admission and readiness

Immediately before start/create/reuse, under the canonical owner, recheck online
CPUs0–71, exact GPU UUID order/totals, current MemAvailable and free GPU memory.
Retain16GiB shared host headroom plus full reviewed caps of target/resident peer,
less each exact resident container's disjoint `anon + shmem` charge. Require
zero swap, validate PID/cgroup and recheck container start identity. Do not credit
RSS, total file cache, THP subsets or memory.current again. An idle peer retains
room to its full cap. New GPU allocation requires accepted peak occupancy plus
reserve; an already resident target/peer requires reserve. This is bounded
point-in-time admission, not reservation against unrelated processes.

Every Ready observation retains exact identity/auth/health/alias checks plus
bounded authenticated native metadata: GLM `/props` at30002; Qwen
`/get_server_info` at30002 or30004 according to the closed profile, never inferred
from port alone. Qwen pool480000/input479994 and optional request479999 must
come from actual top-level/single internal state, not server_args declarations.
Native listeners stay loopback, no redirects/environment proxy/arbitrary URL.

Source tests cover both closed modes, shared affinity, wrong-slot and duplicate
placements, native port/backend selection, resource reuse, required evidence,
Qwen-onGPU0 auth/margin, unknown busy and interruption acknowledgement, peer
survival and GPU1-first boot replay. They establish no actual load, dual-Q
retrieval/performance, LAN service, physical reboot or production acceptance.
Exactly one reviewed temporary near-max Q/Q measured pair precedes receipt
publication and production activation; current keeper remains untouched in PREP.
